const DebugConfig = {
  disableYellowBox: false,
  debugToken: null, // without jwt
  dfDebugNavigation: __DEV__ && false,
  showEvents: false,
  ignoreSafeMode: false,
  disableChat: true,
  selectedServer: 'prod',
};

export default DebugConfig;
