import {Colors} from './colors';

const Theme = {
  darkTheme: {
    text: `${Colors.primary}`,
    background: `${Colors.white}`,
  },
  lightTheme: {
    text: `${Colors.white}`,
    background: `${Colors.black}`,
  },
};

export {Theme};
